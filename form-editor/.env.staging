# Staging form-editor backend (public URL, not a secret). Read by
# `vite build --mode staging`. Fill in once the staging Supabase project exists.
VITE_API_BASE=https://REPLACE_WITH_STAGING_REF.supabase.co/functions/v1/form-editor
